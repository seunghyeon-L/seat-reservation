세세하게 짜기 전에 먼저 짚을 점 3가지부터.

## 지금 일정에서 빠진 거 (발산적 관점)

```
1. Day 0가 없음
   → 환경 셋팅, GitHub, 컨벤션, API 계약 합의는 Day 1에 못 욱여넣음
   → 첫날에 코드 1줄 못 쓰고 끝날 수 있음

2. 통합이 Day 5에 처음 일어남 (이미 늦음)
   → 4명이 5일 동안 따로 만든 거 합치면 100% 충돌남
   → 매일 저녁 통합이 정답

3. 프론트가 백엔드 기다리는 구조
   → 백 1이 Day 2에 선점 API 끝낼 때까지 프론트 1이 못 움직임
   → API 계약 + Mock 데이터로 병렬 진행해야 함
```

이 3개 반영해서 다시 짤게.

---

## Day 0 (목요일 밤 ~ 금요일 새벽) — 발표 직후 2시간

```
[전원 모여서 함께]
21:00-21:30 GitHub 레포 생성 + 브랜치 전략 합의
            - main / develop / feature/<이름>-<기능>
            - PR 머지는 페어 상대가 리뷰
21:30-22:00 폴더 구조 + 네이밍 컨벤션 합의
            - app/ , components/ , lib/ , types/
            - 변수: camelCase, DB컬럼: snake_case
22:00-22:30 API 명세서 4명이 같이 한 번 더 정독
            - 요청/응답 JSON 구조 확정 (← 이게 핵심)
            - Mock 응답 데이터 같이 작성
22:30-23:00 Supabase 프로젝트 멤버 4명 다 초대
            - .env.local 안전하게 공유 (1Password / 디스코드 비공개)
            - Realtime publication ON 확인 (seats 테이블)
```

**Day 0 산출물:** GitHub 레포, 합의된 API 명세 (Mock JSON 포함), 모두가 Supabase 접근 가능

---

## Day 1 (금요일) — DB 스키마 + UI 뼈대

```
[09:00-12:00 오전]
백 1 → seats, reservations 테이블 생성 (SQL Editor에서)
       - 컬럼/제약조건/인덱스 다 잡기
       - 더미 좌석 30개 INSERT (테스트용 시드)
백 2 → Supabase Auth 활성화 (이메일 + 학번 정책 결정)
       - users, penalties 테이블 생성
       - RLS(Row Level Security) 정책 초안
프론트1 → Next.js 프로젝트 셋업 + Tailwind 동작 확인
          - 좌석 배치도 정적 컴포넌트 (하드코딩 30개)
프론트2 → 로그인/회원가입 페이지 라우트 생성
          - 폼 UI만 (로직은 내일)

[13:00-18:00 오후]
백 1 → seats 테이블에 Realtime publication ON
       - SQL Editor에서 SELECT 테스트
       - 동시성 테스트용 SQL 스크립트 미리 작성
백 2 → Auth signUp/signIn 콘솔에서 직접 테스트
       - 더미 유저 5명 생성
프론트1 → 좌석 컴포넌트 props 설계 (status, seatNumber)
          - 색상 토큰 결정 (available=초록, hold=노랑, occupied=빨강)
프론트2 → 회원가입 폼 유효성 검사 (이메일 형식 등)

[저녁 22:00 동기화 - 카톡 30분]
- 각자 PR 올린 거 페어가 리뷰
- 막힌 거 공유, 내일 우선순위 조정
```

**Day 1 완료 기준:** DB 테이블 다 들어감, UI는 데이터 없이도 뜸

---

## Day 2 (토요일) — 좌석 선점 + 인증 (핵심)

```
[09:00-12:00 오전]
백 1 → 좌석 선점 RPC 함수 작성 (낙관적 락)
       - 이미 있는 update().eq().eq() 패턴 그대로
       - 단, holding_until 자동 set (NOW() + 10min)
백 2 → 회원가입/로그인 API 래퍼 작성 (lib/auth.ts)
       - signUp, signIn, signOut, getCurrentUser
프론트1 → Supabase 클라이언트 셋업 + 좌석 데이터 fetch
          - useEffect로 GET /seats
프론트2 → 회원가입 폼 → Supabase Auth 연동
          - 성공 시 리다이렉트, 실패 시 에러 토스트

[13:00-18:00 오후]
백 1 → k6 스크립트 다시 돌려서 검증 (500명 동시)
       - 정확히 1개만 hold 됐는지 확인
백 2 → 로그인 후 세션 관리 (cookie / Supabase session)
       - 미들웨어로 보호 라우트 처리
프론트1 → Realtime 구독 코드 작성
          - seats 테이블 INSERT/UPDATE 감지
          - 색상 실시간 변경 확인
프론트2 → 로그인 후 대시보드 리다이렉트
          - 미인증 상태에서 /dashboard 접근 차단

[저녁 22:00 동기화]
백1+프론트1 → 좌석 선점 end-to-end 한 번 돌려보기 (수동)
백2+프론트2 → 회원가입→로그인→로그아웃 한 번 돌려보기
```

**Day 2 완료 기준:** "회원가입 → 로그인 → 좌석 클릭 → DB에 hold 기록" 한 번이라도 됨

---

## Day 3 (일요일) — 타이머 + 패널티 + 키오스크 화면

```
[09:00-12:00 오전]
백 1 → pg_cron으로 1분마다 만료 좌석 자동 해제
       - SELECT pg_cron.schedule(...)
       - holding_until < NOW() AND status = 'HOLD' 조건
백 2 → 패널티 부여 트리거 또는 함수
       - 자발적 취소 시점에 cancelled_at - start_time 계산
       - 0~5분: 패널티 X, 5~10분: 1회, 노쇼: 2회
프론트1 → 선점 성공 시 타이머 화면
          - 클라이언트는 단순 카운트다운 표시용
          - 진실의 원천은 holding_until (서버)
프론트2 → 키오스크 화면 라우트 (/kiosk)
          - 핀코드 6자리 입력 UI
          - 숫자패드 컴포넌트

[13:00-18:00 오후]
백 1 → 만료 시 patch가 잘 도는지 시간 조작해서 검증
       - holding_until을 5분 전으로 강제 UPDATE 후 cron 대기
백 2 → 패널티 누적 3회 시 is_blocked = true, blocked_until 셋
       - 차단된 유저는 좌석 선점 시도 시 403
프론트1 → 타이머 UI - 1분 남았을 때 경고 색상
          - 만료 시 "선점이 만료되었습니다" 모달
프론트2 → 키오스크 폼 → 핀코드 검증 API 호출 자리만
          - 실제 API 연결은 내일

[저녁 22:00 동기화]
- pg_cron 만료 동작 데모 (백1)
- 패널티 부여 로직 시연 (백2)
- 막힌 부분 공유 (이때 진도 체크)
```

**Day 3 완료 기준:** 10분 만료가 자동으로 풀림, 키오스크 UI는 완성

---

## Day 4 (월요일) — 핀코드 인증 + 마이페이지 (가장 빡센 날)

```
[09:00-12:00 오전]
백 1 → 좌석 조회 API + 선점 취소 API
       - 취소 시 cancelled_at 기록 → 패널티 함수 트리거
       - 내가 hold 중인 좌석만 조회하는 함수
백 2 → 핀코드 검증 API
       - reservations.pin_code 매칭
       - 일치 시 status: HOLD → OCCUPIED
       - 패널티 조회 API
프론트1 → 마이페이지 라우트 (/mypage)
          - 현재 예약 정보, 누적 패널티, 차단 상태
프론트2 → 키오스크 핀코드 입력 → 백 2 API 호출
          - 성공: 확정 화면, 실패: 에러 (남은 시도 횟수)

[13:00-18:00 오후]
백 1 → 페어(프론트1)와 마이페이지 연동 같이 디버깅
백 2 → 페어(프론트2)와 키오스크 흐름 같이 디버깅
프론트1 → 선점 취소 버튼 추가 + 확인 모달
프론트2 → 키오스크 성공 화면 (좌석번호, 확정 시각 표시)

[저녁 22:00 동기화 - 가장 중요]
풀 시나리오 1번 돌려보기 (4명 다같이):
회원가입 → 로그인 → 좌석 클릭 → 핀코드 발급 →
키오스크 입력 → 확정 → 마이페이지 확인
→ 막히는 지점 다 적어두기 (내일 디버깅 대상)
```

**Day 4 완료 기준:** 풀 시나리오가 한 번이라도 끝까지 돈다 (막혀도 OK, 어디서 막히는지만 알면 됨)

---

## Day 5 (화요일) — 통합 + 반응형 + 엣지케이스

```
[09:00-12:00 오전]
전원 → 어제 적어둔 막힌 지점 디버깅
백 1 → 노쇼 시나리오 테스트 (10분 안 가면 자동 해제 + 패널티)
백 2 → 같은 유저가 두 좌석 선점 시도 차단 (DB 제약)

[13:00-18:00 오후]
프론트1 → 모바일 반응형 (좌석 배치도 줌/스크롤)
프론트2 → 키오스크 가로/세로 모드 (태블릿 가정)
백 1 → RLS 정책 마무리 (남의 예약 못 보게)
백 2 → 에러 메시지 한글화 통일

[저녁 19:00-22:00]
전원 → k6 부하 테스트 다시
       - 100명, 500명 시나리오
       - 결과 그래프 캡처 (발표 자료용)
```

**Day 5 완료 기준:** 풀 시나리오 + 엣지케이스 다 돈다, 모바일에서도 됨

---

## Day 6 (수요일) — 발표 자료 + 데모 시나리오 확정

```
[오전]
- 데모 시나리오 3가지 확정
  1) 정상 플로우 (선점→키오스크→확정)
  2) 노쇼 플로우 (10분 만료→패널티)
  3) 동시성 (k6로 500명 동시 클릭→1명만 성공)

[오후]
- 발표 슬라이드 (분담대로)
- 각자 자기 파트 시연 영상 1번 녹화 (백업용)
- 문제 정의 / AS-IS-TO-BE 슬라이드 보강 (교수님 피드백 반영)

[저녁]
- 리허설 1회
- 슬라이드 합치기
- 노트북 / 와이파이 / 전원 체크리스트
```

---

## Day 7 (목요일 오전) — 발표

```
[발표 2시간 전]
- 데모 환경 점검 (Supabase 연결, 시드 데이터 초기화)
- 백업 노트북 1대 준비
- 핀코드 미리 1개 발급해놓기 (데모 실패 대비)

[리허설 2회]
[발표]
```

---

## 매일 22:00 동기화 (의식처럼)

```
1) 오늘 머지된 PR 공유 (각자 1줄)
2) 내일 우선순위 1개씩
3) 막힌 거 (블로커) 있으면 페어한테 SOS
→ 30분 안에 끝내기. 길어지면 그날 진도 안 됨 신호
```

---

## 추가로 던지는 발산적 관점

```
1. 데모 실패 대비책 있음?
   → 발표 중 Supabase 멈추면? 미리 녹화한 시연 영상 준비

2. 핀코드 충돌 가능성
   → 6자리면 100만 경우의 수, 동시 활성 100건이면 충분
   → 단, 발급 시 DB에 unique 검사 한 번 (잠깐 락)

3. RLS 빼먹으면 큰일
   → 다른 학생이 남의 예약 취소시킬 수 있음
   → Day 1에 정책 초안, Day 5에 마무리

4. 데이터 시드 자동화
   → 매일 발표 연습할 때마다 30개 좌석 다시 만들면 시간 낭비
   → reset_seed.sql 하나 만들어두면 30초만에 초기화

5. 강의 스케줄 반영했음?
   → 4명이 같은 강의 듣는 거 아니면 코어타임 잡기 어려움
   → 19:00-22:00을 의무 코어타임으로 (각자 낮 일정 자유)
```

여기까지가 시간 단위 일정. 이중에 더 깊이 들어갈 부분 있어? 

특히 **API 계약(Mock JSON 구조)**이랑 **pg_cron 만료 로직** 둘이 진짜 핵심인데, 둘 중 하나 골라서 더 파볼래?